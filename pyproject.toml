[project]
name = "360chat2api"
version = "0.1.0"
description = "360智脑 to OpenAI API proxy"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "python-dotenv",
    "pydantic",
    "requests>=2.32.4",
]
